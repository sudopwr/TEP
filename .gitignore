# dependencies
node_modules/

# build output
dist/
.tsbuild/
*.tsbuildinfo

# tests
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# secrets
.env
.env.*
!.env.example

# the database and the attached files. `npm run backup` writes into backups/;
# neither folder belongs in version control, and both hold real money data.
data/*
!data/.gitkeep
backups/

# editors / os
.DS_Store
Thumbs.db
